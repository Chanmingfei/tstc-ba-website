{
  "name": "tstc-ba-website",
  "version": "1.0.0",
  "private": true,
  "description": "唐山师范学院吧务组官网（静态站，Tailwind 预编译为本地 CSS，无外部 CDN 依赖）",
  "scripts": {
    "build:css": "tailwindcss -c build/tailwind.config.js -i build/tailwind-input.css -o assets/style.css --minify",
    "build:critical": "python3 build/build-critical-css.py",
    "build": "npm run build:css && npm run build:critical && node build/generate-manifest.js"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.19"
  },
  "dependencies": {
    "@fortawesome/fontawesome-free": "6.7.2"
  }
}
